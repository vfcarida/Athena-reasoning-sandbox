# Athena Reasoning Sandbox — Dependencies
# Python 3.10+ required

# Core ML Framework
torch>=2.0.0
numpy>=1.24.0

# Hugging Face Ecosystem
transformers>=4.35.0
tokenizers>=0.15.0
accelerate>=0.25.0
datasets>=2.16.0

# Fine-Tuning (LoRA / QLoRA)
peft>=0.7.0
trl>=0.7.0

# Quantization (QLoRA) — optional, GPU-only
# bitsandbytes>=0.41.0

# Configuration
pyyaml>=6.0

# Utilities
tqdm>=4.65.0
rich>=13.0.0
deepeval>=0.21.0
boto3>=1.28.0


# Logging (optional)
# wandb>=0.16.0
# tensorboard>=2.15.0

# Agent-Bench Integration (install from local repo)
# pip install -e ../Agent-Bench
